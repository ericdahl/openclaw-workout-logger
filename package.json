{
  "name": "workout-logger",
  "version": "1.0.0",
  "description": "Parse and log workout sessions from Telegram to JSONL database",
  "main": "src/handler.js",
  "scripts": {
    "test": "echo 'Run tests directly: node src/parser.js --dry-run \"/log squat 315x5x3\"'"
  },
  "keywords": [
    "workout",
    "fitness",
    "logging",
    "telegram",
    "parser",
    "jsonl"
  ],
  "author": "Eric",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "config": {
    "requiredEnvVars": ["AUTHORIZED_TELEGRAM_ID"]
  }
}
