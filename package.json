{
  "name": "workout-logger",
  "version": "1.0.0",
  "description": "Parse and log workout sessions from Telegram to JSONL database",
  "main": "src/handler.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "verify-db": "node scripts/verify-db.js",
    "format": "node src/formatter.js"
  },
  "keywords": [
    "workout",
    "fitness",
    "logging",
    "telegram",
    "parser",
    "jsonl"
  ],
  "author": "Eric",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "config": {
    "requiredEnvVars": ["AUTHORIZED_TELEGRAM_ID"]
  }
}
